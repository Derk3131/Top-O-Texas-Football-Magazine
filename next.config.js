/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // If you had any specific configurations, make sure they are valid JavaScript.
  // For example, if you had reactStrictMode: true, it would look like:
  // reactStrictMode: true,
};

module.exports = nextConfig;
